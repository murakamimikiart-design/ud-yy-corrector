# 当日までの手引き（UDトーク × YYprobe 修正補助）

**最終更新：2026年9月9日**
このファイルは、間が空いたあとや当日に読み返すためのもの。
細かい仕様は `README.md`、実装の意図は各ソースのコメントにある。

---

## 1. いま何が動くか

**Chrome拡張として動く。UDもYYもブラウザから自動で読み込み、1発話ずつ直して貼る。**

```
UDトーク公開ページ ─┐
                    ├→ 拡張機能が両方読む → 1発話ずつ比較 → 直した文をコピー → UDトークに貼る
YYProbe配信ページ ─┘
```

コピー＆ペーストで持ち込む作業はもう無い。人がやるのは
**「どちらが正しいか選ぶ」**と**「決めた文をUDトークに貼る」**の2つだけ。

---

## 2. 当日の準備

### 一度だけ：拡張機能を入れる
1. Chrome で `chrome://extensions` を開く
2. 右上の「デベロッパー モード」をON
3. 「パッケージ化されていない拡張機能を読み込む」→ `UD_YY/chrome_extension` フォルダを選ぶ

※ 拡張機能を更新（⟳）したときは、**開いているページを必ず ⌘R で読み込み直す**。
忘れると連携が切れる（切れた場合は画面に橙色で知らせる）。

### 毎回：2つのページを開く
| | 出し方 | 開いた合図 |
|---|---|---|
| UDトーク | トーク公開画面の「ウェブで公開」→ URL | 右下に比較パネルが出る |
| YYProbe | 文字起こし画面 **左上のQRアイコン** → URL | 左下に緑で「YY→UD 送信中」 |

どちらもパスコード／パスワードを聞かれたら自分で入力する。
YY側のタブは裏に回っていてよい。

---

## 3. 当日の操作

```
① 直す発話を選ぶ      パネル上段のUD発話一覧をクリック
② 対応するYYが出る     その発話に当たるYYの箇所だけが自動で切り出される
③ 食い違いを選ぶ       [UDトーク案] [YYprobe案] [✎自分で入力] から選ぶ
④ 決定してコピー       その発話だけがコピーされる → UDトークで貼り替える
```

- **直している間も取り込みは止まらない。** 選んだ発話は流れても一覧に残る
- **貼り付けるのは常に1発話ぶん。** 前後の発話は混ざらない
- 「全画面」でブラウザいっぱいに広がる（文字も大きくなる）
- 「言い回しも表示」で、隠してある軽微な差（「ね、」「って」等）も見られる

### 画面の色
| 色 | 意味 |
|---|---|
| 橙 | UDトーク側 |
| 緑 | YYprobe側 |
| 青 | 自分で入力した語 |

---

## 4. うまくいかないとき

| 症状 | 対処 |
|---|---|
| パネルが出ない | ページを ⌘R。それでも駄目なら `option + U` |
| YY側が入らない | YYの配信ページを開いているか確認。左下のバッジが緑か見る |
| バッジが橙色 | 拡張機能を更新した後。そのページを ⌘R |
| 「対応する箇所が見つかりません」 | YYがまだその発話を認識していない。少し待つ |
| 候補が多すぎる | 「言い回しも表示」をOFFのままにする（既定でOFF） |
| どうにもならない | **ツールを閉じて、UDトーク編集画面で直接手直しする。** 字幕配信そのものには影響しない |

**最後の行が一番大事。** このツールは補助であり、無くても運用は止まらない。

---

## 5. 残っている課題

### 本番前に必ず
- [ ] **YY配信ページのドメイン確認。** 検証時は `analytics.yyprobe-demo.com` だった（demo が入っている）。
      本番アカウントで違うドメインになるなら `chrome_extension/manifest.json` の
      `matches` に1行足して拡張機能を再読み込みする

### やると効く
- [ ] **UDトークの辞書に単語登録。** 実データでは「手話学習者→車学習者」「コクリハ→送り歯」など、
      この分野の固有名詞ほど両方が外していた。事前登録すれば直す件数そのものが減る
- [ ] **1サイクルの所要時間を測る。** 実データでは30秒の発話に要判断10箇所だった。
      全部は追いつかない前提で、何を優先して直すか決めておく
- [ ] 登壇者名簿・資料を手元に置く（正誤の判断が速くなる）

---

## 6. ファイル構成

```
chrome_extension/            ★本番で使う（Chromeに読み込ませる）
  manifest.json              対象ページの指定
  panel.js                   比較パネル（自動生成）
  yy.js                      YY側の読み取り（自動生成）
udtalk_live_panel.js         パネルの本体。編集するならここ
yy_reader.js                 YY読み取りの本体
build_extension.command      上2つを chrome_extension/ に反映する

uy_corrector.html            ブラウザ版（拡張が使えないときの手貼り版）
UD取り出しボタン.html         公開ページからテキストを取り出すブックマークレット
diff_engine.py               差分ロジックのPython版（テスト用）
test_diff_engine.py          単体テスト13件。python3 test_diff_engine.py
testdata/real/               実データ（2026/09/09 の UD・YY 出力）
legacy_pysimplegui_prototype.py  初日のプロトタイプ（参考）
```

---

## 7. 設計で外せない点（作り替えるとき用）

1. **YYは正解ではない。** 実際にUDが正しいこともYYが誤ることもある。だから三択にしてある
2. **1発話単位で直す。** UDとYYは発話の区切りが違うので、まとめて直すと貼付時に文が重複する
3. **対応箇所の切り出しは全発話まとめて行う。** 発話ごとに探すと範囲が前後にはみ出す
4. **直している間に取り込みを止めない。** 止めると遅れを取り戻せなくなる
5. **判断するのは人。** ツールは食い違いを示すだけで、正誤は決めない

---

**目指すこと：** 9月13日、参加者に読める字幕を届ける。
ツールが動かなくても手直しで回るように、必ず逃げ道を残しておく。
